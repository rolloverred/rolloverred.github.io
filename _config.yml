domain: rolloverred.github.io       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://rolloverred.github.io  # the base hostname and protocol for your site, e.g. http://example.com
# baseurl: /      # place folder name if the site is served in a subfolder
show_excerpts: true


title: Roll Over Red
#email:  fourblumutts@gmail.com
description: >- # this means to ignore newlines until "baseurl:"
  Roll Over Red is an American brand. We unify people around honest politics. 
baseurl: "" # the subpath of your site, e.g. /blog
url: "http://rolloverred.com" # the base hostname & protocol for your site, e.g. http://example.com
instagram_username: roll.over.red
# bluesky_username: rollovered
# github_username:  jekyll

# Build settings
theme: minima
plugins:
  - jekyll-feed
  - jekyll-seo-tag

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
